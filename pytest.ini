[pytest]
testpaths = tests
markers =
    smoke: Fast, critical-path tests (target: <90s total)
    regression: Full regression suite
    integration: Tests requiring live server + Supabase
    security: Security regression tests
addopts = --tb=short -q
